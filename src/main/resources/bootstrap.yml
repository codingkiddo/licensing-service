spring:
    application:
     name: licensing-service 
    profiles:
      active: dev
    cloud:
        config: 
            uri: http://configserver:8071
            
  
management:
  endpoints:
    web:
      exposure:
        include:
        - health,info,refresh
  endpoint:
    restart:
      enabled: true
    refresh:
      enabled: true